[project]
name = "popi-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests", "pandas", "numpy"
]

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
