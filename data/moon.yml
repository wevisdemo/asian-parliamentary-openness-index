language: "python"
layer: "application"

tasks:
  build:
    command: "uv run main.py"
    inputs:
      - "main.py"
      - "pyproject.toml"
    outputs:
      - "output"
  lint:
    command: "uv run ruff check ."
  format:
    command: "uv run ruff format ."
