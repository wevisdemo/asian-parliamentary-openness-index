[project]
name = "popi-scripts"
version = "0.1.0"
description = "One-off maintenance scripts for the Asia Parliament Openness Index"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
